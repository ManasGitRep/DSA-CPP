#include <iostream>
using namespace std;
int main()
{
    int arr[100], n;
    cout << "enter the size of an array:" << endl;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}